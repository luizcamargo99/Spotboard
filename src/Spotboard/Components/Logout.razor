<SpotifyButton OnClick="async () => { await LogoutAsync(); }">
    LOG OUT
</SpotifyButton>

@code {

    async Task LogoutAsync()
    {
        await authRepository.SaveAsync(null);
        navigationManager.NavigateTo("/", replace: true);
    }
}
